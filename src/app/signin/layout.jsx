export const metadata = {
    title: "QurbaniHat | Signin",
    description: "Sign in to choose from healthy livestock Cows, Bulls, Goats & Sheep and book online.",
};

const SignInLayout = ({children}) => {
    return <>{children}</>;
}
 
export default SignInLayout;